# 第一版：照片记录 NFC 与 VR 重现 App 项目总览

更新时间：2026-06-06

本文档用于汇总当前项目已经完成的内容、仍需完成的内容、三人分工、技术栈选择和下一阶段执行顺序。  
原有文档不做覆盖，本文件为重新创建的新版本文档。

## 1. 项目目标

本项目是一个照片记录 App。用户授权读取本地照片后，App 可以基于照片的地区、时间、种类进行整理，并在地图上展示照片位置。后续照片、地点或相册可以与 NFC 内容进行绑定，最终形成 VR 视频或沉浸式场景重现。

第一版目标不是一次性完成全部商业化功能，而是先完成 App 工程基础、地图首页、本地照片权限、照片地图标点、Mapbox 地图接入、NFC/VR 页面入口和后续开发路径。

## 2. 当前技术栈

### 2.1 前端与 App

- Flutter：跨平台 App 主框架
- Dart：主要开发语言
- go_router：页面路由
- flutter_riverpod：状态管理
- flutter_map：地图组件
- latlong2：地图坐标类型
- photo_manager：读取用户本地相册与照片元数据
- nfc_manager：NFC 能力预留与后续接入
- video_player：VR/视频播放能力预留
- cached_network_image：远程图片缓存预留
- photo_view：照片查看能力预留

### 2.2 地图与配置

- Mapbox Static Tiles API：地图瓦片服务
- `MAPBOX_ACCESS_TOKEN`：通过 Flutter `--dart-define` 注入，不写入仓库
- `MAPBOX_STYLE`：可选地图样式，默认 `mapbox/streets-v12`
- `API_BASE_URL`：通过 `--dart-define` 注入后端地址

### 2.3 后端与数据库建议

- 后端框架建议：Node.js + NestJS，或 Python + FastAPI
- 主数据库建议：PostgreSQL
- 地理位置扩展建议：PostGIS
- 缓存与任务队列建议：Redis
- 文件存储建议：S3 兼容对象存储
- 主要存储对象：照片元数据、缩略图、NFC 绑定关系、VR 生成任务、VR 视频文件

### 2.4 本地与平台

- Android SDK：已可用于构建 Android APK
- Windows Desktop：已可构建并运行
- Web：已可构建并本地预览
- iOS：Flutter 工程已生成，后续需在 macOS/Xcode 环境验证

## 3. 三人分工

### 3.1 第一部分：前端与应用

负责范围：

- Flutter App 工程
- 首页地图
- 本地照片权限
- 照片标点与缩略图
- NFC 读取、写入与绑定页面
- VR 播放或沉浸式重现页面
- App 运行、打包、真机调试

当前已完成：

- 创建 Flutter 项目
- 生成 Android、iOS、Web、Windows 平台工程
- 建立基础目录结构
- 建立首页、照片、NFC、VR 页面入口
- 首页改为完整地图视图
- 接入本地照片权限读取逻辑
- 支持在地图上显示带 GPS 信息的照片位置
- 支持在标点附近显示照片缩略图
- 接入 Mapbox 地图瓦片
- 完成 Web、Android APK、Windows 构建

后续需要完成：

- Android 真机授权测试
- Mapbox token 真实环境验证
- 照片按时间、地区、种类筛选
- 照片详情页
- 相册或照片集合管理
- NFC 标签读取、写入、绑定
- VR 视频或沉浸式页面真实播放逻辑
- 异常状态与空状态细化
- UI 交互细节优化

### 3.2 第二部分：后端与数据库

负责范围：

- 后端 API
- 用户与鉴权
- 照片元数据同步
- 地点、分类、时间轴数据
- NFC 绑定关系
- VR 生成任务
- 文件存储与访问权限

当前已完成：

- 前端已预留 `API_BASE_URL`
- 前端已引入 `dio`，具备后续 API 对接基础
- 前端功能已按照片、NFC、VR 模块拆分，便于接口对接

后续需要完成：

- 选择并初始化后端框架
- 设计数据库表结构
- 实现用户、照片、地点、分类、NFC、VR 任务接口
- 实现照片元数据同步接口
- 实现 NFC 与照片、相册、地点的绑定接口
- 实现 VR 视频生成任务创建、查询、回调与结果保存
- 实现对象存储上传与访问控制
- 实现接口鉴权、权限校验和数据安全策略

### 3.3 第三部分：数据库、应用题与交互

负责范围：

- 数据模型设计
- 分类规则
- 用户流程
- 信息架构
- 页面交互
- 演示流程与黑客松展示材料

当前已完成：

- 明确核心数据对象：照片、地区、时间、种类、NFC、VR 重现
- 明确首页以地图为主视觉
- 明确照片用地图标点与缩略图呈现
- 明确三人分工方向
- 完成第一版页面骨架

后续需要完成：

- 定义照片分类规则
- 定义地点聚合规则
- 设计时间轴浏览方式
- 设计 NFC 标签绑定流程
- 设计 VR 重现生成流程
- 设计照片详情、筛选、地图聚合交互
- 输出数据库字段说明
- 输出接口字段说明
- 整理黑客松演示脚本

## 4. 已完成工作清单

### 4.1 文档与计划

- 已生成前端第一阶段步骤文档
- 已生成前端运行记录文档
- 已生成本地照片地图改造文档
- 已生成 Mapbox 首页地图改造文档
- 已按前端、后端数据库、应用交互三部分拆分任务
- 已形成第一版项目总览文档

### 4.2 Flutter 工程

- 已在 GitHub 仓库中创建 Flutter 项目
- 已生成 Android、iOS、Web、Windows 平台工程
- 已配置基础主题、路由和页面框架
- 已建立模块化目录结构
- 已引入地图、状态管理、照片权限、NFC、视频等依赖
- 已完成多次 Git 提交并推送到 GitHub

当前 Git 提交进度：

```text
54a466c Use Mapbox tiles for photo map
1e191fc Show local photos on map
3cf0d78 Implement frontend foundation stage
435888e Add Flutter platform projects
10634f6 Create Flutter app scaffold
d6a2d96 Initial commit
```

### 4.3 工具链与运行

- 已安装或配置 Flutter SDK
- 已检查并使用 Android SDK
- 已处理 Windows 构建所需 Visual Studio/Ninja 配置
- 已成功构建 Web
- 已成功构建 Android debug APK
- 已成功构建 Windows 桌面应用
- 已启动本地 Web 预览
- 已启动 Windows 应用

当前本地 Web 预览地址：

```text
http://127.0.0.1:5126/
```

Android APK 输出：

```text
build\app\outputs\flutter-apk\app-debug.apk
```

Windows 可执行文件：

```text
build\windows\x64\runner\photo_link_vr.exe
```

### 4.4 首页地图与照片能力

- 首页已改为完整地图
- 顶部 AppBar 已在首页隐藏
- 底部导航保留
- Mapbox 地图瓦片已接入
- Mapbox token 通过 `MAPBOX_ACCESS_TOKEN` 注入
- 本地照片权限读取逻辑已接入
- Android 读取照片权限已配置
- iOS 相册权限说明已配置
- 支持读取带 GPS 的照片
- 支持将照片以地图标点展示
- 支持在标点附近展示缩略图
- 未配置 Mapbox token 时显示提示，不请求空 token

### 4.5 验证结果

已通过以下验证：

```powershell
dart format lib test
flutter analyze
flutter test
flutter build web
flutter build apk --debug
flutter build windows
```

当前限制：

- 本机环境当前未设置 `MAPBOX_ACCESS_TOKEN`
- 未设置 token 时首页显示配置提示
- Android 真机照片授权与 GPS 照片读取还需在真实设备上验证
- iOS 还未在 Xcode 环境验证

## 5. 需要完成的工作清单

### 5.1 近期优先级

1. 配置真实 Mapbox token 并重新运行 App
2. 使用 Android 真机测试本地照片权限
3. 准备带 GPS 信息的照片进行地图标点测试
4. 完成照片详情页
5. 完成照片按时间、地区、种类筛选
6. 完成 NFC 读取入口
7. 完成 NFC 与照片或地点的绑定流程
8. 完成 VR 视频播放或重现页面的真实数据接入

### 5.2 前端待完成

- Mapbox token 配置说明页面或开发文档
- 地图缩放、聚合、定位按钮
- 照片详情页
- 照片筛选面板
- 时间轴视图
- 地区聚合视图
- 种类标签管理
- NFC 扫描页面
- NFC 写入页面
- NFC 绑定结果页面
- VR 视频播放页面
- VR 任务状态页面
- 登录或用户信息入口
- 离线、无权限、无网络、无照片等状态处理

### 5.3 后端待完成

- 后端项目初始化
- 数据库初始化
- 用户认证接口
- 照片元数据上传接口
- 照片查询接口
- 地点查询接口
- 分类查询接口
- NFC 绑定接口
- VR 任务创建接口
- VR 任务查询接口
- VR 结果保存接口
- 文件上传接口
- 接口鉴权与权限控制

### 5.4 数据库待完成

建议核心表：

- `users`：用户
- `photos`：照片元数据
- `photo_locations`：照片地理位置
- `albums`：相册或照片集合
- `categories`：照片种类
- `nfc_tags`：NFC 标签
- `nfc_bindings`：NFC 绑定关系
- `vr_jobs`：VR 生成任务
- `vr_assets`：VR 视频或场景资源

需要继续明确：

- 字段类型
- 索引策略
- 地理位置查询方式
- 任务状态流转
- 用户权限模型
- 照片隐私与存储策略

### 5.5 交互与展示待完成

- 黑客松演示路径
- 首次授权流程
- 首页地图默认视角
- 地图标点点击效果
- 照片详情展示布局
- NFC 标签绑定流程说明
- VR 重现生成流程说明
- 演示用样例数据
- 项目介绍 PPT 或展示脚本

## 6. 推荐后续执行顺序

### 阶段一：让当前 App 在真机跑通

1. 准备 Mapbox token
2. 用 token 重新运行 Android/Web
3. Android 真机授权相册权限
4. 使用带 GPS 的照片验证地图标点
5. 修复真机兼容问题

### 阶段二：完善照片记录核心体验

1. 增加照片详情页
2. 增加时间筛选
3. 增加地区筛选
4. 增加种类标签
5. 增加地图标点聚合

### 阶段三：接入 NFC

1. 读取 NFC 标签
2. 写入 NFC 标签
3. 建立 NFC 与照片、相册、地点的绑定关系
4. 从 NFC 打开对应照片记忆页面

### 阶段四：接入后端与数据库

1. 初始化后端项目
2. 建立数据库模型
3. 对接照片元数据同步
4. 对接 NFC 绑定接口
5. 对接 VR 任务接口

### 阶段五：实现 VR 重现

1. 设计 VR 重现数据结构
2. 接入视频播放或全景展示
3. 生成或加载 VR 场景资源
4. 从照片集合生成重现入口
5. 打通 NFC 到 VR 重现的完整流程

## 7. 常用运行命令

Web 调试：

```powershell
flutter run -d chrome --dart-define=MAPBOX_ACCESS_TOKEN=你的MapboxToken
```

Android Debug APK：

```powershell
flutter build apk --debug --dart-define=MAPBOX_ACCESS_TOKEN=你的MapboxToken
```

Windows 构建：

```powershell
flutter build windows --dart-define=MAPBOX_ACCESS_TOKEN=你的MapboxToken
```

质量检查：

```powershell
dart format lib test
flutter analyze
flutter test
```

## 8. 第一版结论

当前项目已经完成从 0 到 1 的基础搭建：Flutter 工程、平台工程、首页地图、本地照片权限、Mapbox 接入、照片标点、缩略图展示、NFC/VR 页面入口和基础构建验证都已完成。

下一步重点是用真实 Mapbox token 和 Android 真机跑通照片地图，然后继续完成照片筛选、NFC 绑定和 VR 重现主流程。
